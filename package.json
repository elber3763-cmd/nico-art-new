{
  "name": "dein-projekt",
  "version": "1.0.0",
  "scripts": {
    "dev": "tinacms dev -c \"npm run next:dev\"",
    "next:dev": "next dev",
    "build": "tinacms build && next build",
    "start": "next start",
    "export": "next export"
  },
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "tinacms": "^2.0.0",
    "tinacms-authjs": "^1.0.0"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.6.0",
    "typescript": "^5.0.0"
  }
}
